{
  "private": true,
  "scripts": {
    "test": "yarn workspaces foreach run test --color",
    "lint": "yarn workspaces foreach run lint --color",
    "build": "yarn workspaces foreach run build --color"
  },
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@3.8.1"
}
